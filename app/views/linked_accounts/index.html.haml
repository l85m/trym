.modal-dialog.modal
  .modal-content.vertical-center    
    .modal-body.text-center
      %h2{style: "margin-bottom: 20px;"} Past Account Scans
      - @linked_accounts.each do |request|
        %p.lead= link_to "#{request.financial_institution.name} (scanned on #{request.created_at.strftime('%A %B %e, %Y')})", edit_linked_account_path(request)

      
      .text-center{style: "margin-top: 30px;"}
        .inline-block{style: "width: 130px;"}=link_to "cancel", charges_path, class: "btn btn-default btn-lg btn-block"
        .inline-block{style: "width: 130px;"}=link_to "new scan", new_linked_account_path, class: "btn btn-primary btn-lg btn-block", remote: true