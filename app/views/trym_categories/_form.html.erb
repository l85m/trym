<%= form_for(@trym_category) do |f| %>
  <% if @trym_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trym_category.errors.count, "error") %> prohibited this trym_category from being saved:</h2>

      <ul>
      <% @trym_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :recurring %><br>
    <%= f.check_box :recurring %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
