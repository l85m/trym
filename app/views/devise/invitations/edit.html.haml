.row
  %h1.text-center Welcome to Trym!
  .col-lg-5.col-lg-offset-2.col-md-6.col-md-offset-1.col-xs-10.col-xs-offset-1
    %h3
      Your less than 10 minutes away from:
    %ul.fa-ul.lead
      %li 
        %i.fa-li.fa.fa-binoculars
        Discovering the recurring expenses you're paying for, even those you've forgotten about
      %li 
        %i.fa-li.fa.fa-money
        Finding better deals on the services you want to keep using
      %li 
        %i.fa-li.fa.fa-phone-square
        Canceling all future payments on the services you don't need anymore

  .col-lg-3.col-md-4.col-md-offset-0.col-xs-10.col-xs-offset-1
    %h3= t "devise.invitations.edit.header"
    = simple_form_for resource, :as => resource_name, :url => invitation_path(resource_name), :html => {:method => :put} do |f|
      = f.hidden_field :invitation_token
        
      .form-inputs
        = f.input :password, input_html: { class: "text-center" }
        = f.input :password_confirmation, input_html: { class: "text-center" }

      = f.button :submit, "create my account", class: "btn btn-primary"